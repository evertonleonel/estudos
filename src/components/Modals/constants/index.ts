const alocation = `Solicitar Alocação`;
const apllication = `Visualizar candidatura`;
const similarAlocation = `Alocação similar`;
const confirmAction = `Confirmar Ação`;
const newAlocation = `Solicitar nova alocação`;

export const titles = {
  modals: {
    alocation,
    apllication,
    similarAlocation,
    confirmAction,
    newAlocation,
  },
};
